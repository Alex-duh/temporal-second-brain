# ── Personal source material — never push these ───────────────────
raw/

# ── Generated knowledge base — stays local ────────────────────────
wiki/

# ── Personal outputs — stay local ─────────────────────────────────
# Your real heatmap and query results never get pushed
outputs/

# ── Ingestion tracking — local only ──────────────────────────────
raw/.ingested
raw/processed_files.json

# ── Claude conversation exports ────────────────────────────────────
conversations.json
memories.json
projects.json
users.json

# ── Personal documents ─────────────────────────────────────────────
*.pdf
*.docx
*.pptx

# ── Obsidian — commit config, ignore personal workspace state ─────
.obsidian/workspace.json
.obsidian/workspace-mobile.json
Welcome.md

# ── System files ──────────────────────────────────────────────────
.DS_Store
